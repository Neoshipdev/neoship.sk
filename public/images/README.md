# Obrázky

Sem vložte podkladové obrázky, napr. `Podklad_stvorec.png` (hero gradient
podklad), screenshoty dashboardu a fotografie pre blog.

Hero gradient je aktuálne riešený CSS-om (`bg-hero-gradient` v
`tailwind.config.ts`), takže obrázok podkladu nie je nutný.
